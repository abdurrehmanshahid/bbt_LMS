line-length = 100
target-version = "py311"

[lint]
select = ["E", "F", "W", "I", "UP", "B"]
ignore = ["E501", "B008"]  # line length handled by formatter; B008 = function calls in defaults (FastAPI pattern)

[lint.per-file-ignores]
"tests/*" = ["S101"]  # allow assert in tests
