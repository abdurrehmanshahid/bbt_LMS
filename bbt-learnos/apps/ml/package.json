{
  "name": "@bbt/ml",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "uvicorn main:app --reload --port 8000",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "test": "pytest",
    "test:ci": "pytest --cov=. --cov-report=xml",
    "lint": "ruff check .",
    "lint:fix": "ruff check . --fix",
    "type-check": "echo 'Python type checking runs via mypy in CI — skipping in JS pipeline'"
  }
}
